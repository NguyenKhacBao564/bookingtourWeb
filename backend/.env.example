# .env.example (backend)
# Copy file này thành .env thực tế, rồi cập nhật các giá trị bên dưới

# --- Server config ---
PORT=3001
# --- Database MSSQL config ---
DB_HOST=localhost
DB_USER=sa
DB_PASS="nguyenKhacBao123"
DB_NAME=master

# --- JWT Secret (nếu bạn dùng JWT) ---
JWT_SECRET=someRandomSecretKey

# --- OAUTH config (nếu có) ---
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxx
FACEBOOK_APP_ID=xxx
FACEBOOK_APP_SECRET=xxx

# --- Payment keys (nếu có cổng thanh toán) ---
PAYPAL_CLIENT_ID=xxx
PAYPAL_CLIENT_SECRET=xxx
VN_PAY_TMN_CODE=xxx
VN_PAY_HASH_SECRET=xxx

# --- Email or other 3rd party service ---
SENDGRID_API_KEY=xxx
